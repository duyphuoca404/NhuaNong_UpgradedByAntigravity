<UserControl x:Class="NhuaNong.Wpf.UserControls.WeighControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="80">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Weight Value (spinEdit1) -->
        <Border Background="Black" BorderBrush="Gray" BorderThickness="1" Grid.Row="0" Margin="2">
            <TextBlock Text="{Binding Weight, RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat={}{0:N0}}" 
                       Foreground="Lime" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center" Padding="4"/>
        </Border>

        <!-- Scale Image (picWeigh) -->
        <Border Grid.Row="1" Margin="2">
            <materialDesign:PackIcon Kind="ScaleBalance" Width="40" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray"/>
        </Border>

        <!-- Status Indicator (picLebEmpty) -->
        <Border Grid.Row="2" Height="10" Background="{Binding StatusColor, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="2" CornerRadius="2"/>
    </Grid>
</UserControl>
