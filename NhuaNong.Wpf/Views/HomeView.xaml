<UserControl x:Class="NhuaNong.Wpf.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <materialDesign:Card Margin="16" Padding="32" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Style="{DynamicResource MaterialDesignHeadline4TextBlock}" Text="{Binding Title}" />
                <TextBlock Style="{DynamicResource MaterialDesignBody1TextBlock}" Text="Welcome to the new Dashboard." Margin="0,16,0,0" />
                
                <Grid Margin="0,24,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    
                    <!-- Stats Cards Placeholder -->
                    <materialDesign:Card Grid.Column="0" Margin="0,0,8,0" Padding="16" Background="{DynamicResource PrimaryHueLightBrush}" Foreground="{DynamicResource PrimaryHueLightForegroundBrush}">
                        <StackPanel>
                            <materialDesign:PackIcon Kind="ChartBar" Width="32" Height="32" />
                            <TextBlock Text="Sản Lượng" Style="{DynamicResource MaterialDesignHeadline6TextBlock}" Margin="0,8,0,0" />
                            <TextBlock Text="{Binding TotalProduction, StringFormat={}{0:N2} tấn}" Style="{DynamicResource MaterialDesignHeadline4TextBlock}" />
                        </StackPanel>
                    </materialDesign:Card>

                    <materialDesign:Card Grid.Column="1" Margin="8,0,8,0" Padding="16" Background="{DynamicResource SecondaryHueLightBrush}" Foreground="{DynamicResource SecondaryHueLightForegroundBrush}">
                        <StackPanel>
                            <materialDesign:PackIcon Kind="Truck" Width="32" Height="32" />
                            <TextBlock Text="Chuyến Xe" Style="{DynamicResource MaterialDesignHeadline6TextBlock}" Margin="0,8,0,0" />
                            <TextBlock Text="{Binding TotalTrips, StringFormat={}{0:N0} chuyến}" Style="{DynamicResource MaterialDesignHeadline4TextBlock}" />
                        </StackPanel>
                    </materialDesign:Card>

                    <materialDesign:Card Grid.Column="2" Margin="8,0,0,0" Padding="16">
                        <StackPanel>
                            <materialDesign:PackIcon Kind="AlertCircleOutline" Width="32" Height="32" Foreground="OrangeRed" />
                            <TextBlock Text="Cảnh Báo" Style="{DynamicResource MaterialDesignHeadline6TextBlock}" Margin="0,8,0,0" />
                            <TextBlock Text="{Binding ActiveAlerts, StringFormat={}{0:N0} active}" Style="{DynamicResource MaterialDesignHeadline4TextBlock}" />
                        </StackPanel>
                    </materialDesign:Card>
                </Grid>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
