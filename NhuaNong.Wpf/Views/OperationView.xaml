<UserControl x:Class="NhuaNong.Wpf.Views.OperationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024">
    <Grid>
        <materialDesign:Card Margin="16" Padding="32" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Grid>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <materialDesign:PackIcon Kind="Factory" Width="64" Height="64" HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}" />
                    <TextBlock Text="Giao diện Vận Hành (Mimic Diagram)" Style="{DynamicResource MaterialDesignHeadline4TextBlock}" HorizontalAlignment="Center" Margin="0,16,0,0" />
                    <TextBlock Text="Khu vực này sẽ hiển thị sơ đồ trạm trộn, Silo, Băng tải, v.v. y hệt như ứng dụng cũ." Style="{DynamicResource MaterialDesignBody1TextBlock}" HorizontalAlignment="Center" Margin="0,8,0,0" />
                    <TextBlock Text="(Đang phát triển)" Style="{DynamicResource MaterialDesignCaptionTextBlock}" HorizontalAlignment="Center" Margin="0,8,0,0" FontStyle="Italic" />
                </StackPanel>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
