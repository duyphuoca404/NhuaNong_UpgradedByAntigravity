<UserControl x:Class="NhuaNong.Wpf.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <materialDesign:Card Margin="16" Padding="32" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Style="{DynamicResource MaterialDesignHeadline4TextBlock}" Text="{Binding Title}" />
                <TextBlock Style="{DynamicResource MaterialDesignBody1TextBlock}" Text="Application Configuration" Margin="0,16,0,0" />
                
                <StackPanel Margin="0,24,0,0">
                    <TextBox materialDesign:HintAssist.Hint="Server Name" Style="{DynamicResource MaterialDesignFloatingHintTextBox}" Margin="0,8" />
                    <TextBox materialDesign:HintAssist.Hint="Database Name" Style="{DynamicResource MaterialDesignFloatingHintTextBox}" Margin="0,8" />
                    <Button Content="Save Settings" HorizontalAlignment="Left" Margin="0,16,0,0" />
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
